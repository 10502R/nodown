# 개발스택(복사본)

# 최종 추천 스택

```
Python
+ Flask
+ Jinja2
+ HTML/CSS
+ Vanilla JavaScript
+ Bootstrap
+ JSON 합성데이터
+ LLM API
+ CLOVA OCR API
+ Python 규칙 엔진
+ GitHub
+ Vercel 또는 Render 배포
```

단, ‘그냥 HTML’만으로는 부족함. AI와 OCR API 키를 숨기고 파일을 처리하려면 Python 서버가 필요함. 화면 전환과 로딩 처리에는 소량의 JavaScript도 필요함.

# Flask가 적합한 이유

## 1. 팀이 Python·HTML을 사용할 예정임

```
Python
→ 카드거래 필터링
→ OCR 호출
→ LLM 호출
→ 규칙 판정
→ 제출자료 생성

HTML
→ 거래 탐지 화면
→ 자료 입력 화면
→ 분석 결과 화면
```

역할이 명확함.

## 2. 하나의 프로젝트로 구현 가능함

Flask에서도 프론트와 백엔드를 하나의 저장소로 관리할 수 있음.

```
Flask 서버
├─ HTML 화면 제공
├─ JSON 데이터 읽기
├─ OCR API 호출
├─ LLM API 호출
├─ 규칙 엔진 실행
└─ 결과문서 생성
```

별도의 React 프로젝트나 Node.js 서버가 필요하지 않음.

## 3. 현재 서비스 규모에 충분함

이번 MVP는 다음 정도임.

- 화면 약 7개
- 대표 사례 3개
- JSON 합성데이터
- OCR API 1개
- LLM API 1개
- 규칙 엔진
- 결과 출력

대규모 프론트엔드 상태관리나 데이터베이스가 필요한 서비스가 아니므로 Flask로 충분함.

# 구체적인 기술 구성

| 구분 | 사용할 기술 |
| --- | --- |
| 백엔드 | Python Flask |
| HTML 렌더링 | Jinja2 |
| 프론트 | HTML·CSS·Vanilla JavaScript |
| 공통 디자인 | Bootstrap |
| 합성데이터 | JSON |
| 임시 화면 데이터 | 브라우저 sessionStorage |
| AI | LLM API |
| OCR | CLOVA OCR API |
| 규칙 판정 | Python 함수 |
| HTTP 요청 | `requests` 또는 `httpx` |
| 환경변수 | `python-dotenv` |
| 서버 실행 | Gunicorn |
| 코드 테스트 | pytest |
| 형상관리 | GitHub |

# 권장 폴더 구조

```
project/
├─ app.py
├─ requirements.txt
├─ .env
├─ .gitignore
│
├─ routes/
│  ├─ detection.py
│  ├─ evidence.py
│  ├─ analysis.py
│  └─ result.py
│
├─ services/
│  ├─ case_service.py
│  ├─ ocr_service.py
│  ├─ llm_service.py
│  ├─ rule_service.py
│  └─ report_service.py
│
├─ templates/
│  ├─ index.html
│  ├─ detection.html
│  ├─ card_app.html
│  ├─ case_confirm.html
│  ├─ evidence.html
│  ├─ analysis.html
│  └─ result.html
│
├─ static/
│  ├─ css/
│  │  └─ style.css
│  ├─ js/
│  │  ├─ evidence.js
│  │  └─ analysis.js
│  └─ images/
│
├─ data/
│  ├─ transactions.json
│  ├─ demo_cases.json
│  ├─ fallback_evidence.json
│  └─ fallback_analysis.json
│
├─ prompts/
│  ├─ analyze_evidence.txt
│  └─ generate_submission.txt
│
└─ tests/
   ├─ test_detection.py
   ├─ test_rules.py
   └─ test_scenarios.py
```

# 팀원별 코드 분리

Flask의 Blueprint 기능을 사용하면 각자 파일을 나눠서 작업할 수 있음.

## A

```
routes/detection.py
services/case_service.py
services/rule_service.py
templates/detection.html
templates/card_app.html
data/transactions.json
```

담당 기능은 다음과 같음.

- 거래 탐지
- 가맹점 상태 확인
- 사례 생성
- 규칙 엔진
- 카드사 알림

## B

```
routes/evidence.py
services/ocr_service.py
templates/case_confirm.html
templates/evidence.html
static/js/evidence.js
```

담당 기능은 다음과 같음.

- 소비자 상황 확인
- 파일 업로드
- OCR
- 핵심정보 추출

## C

```
routes/analysis.py
services/llm_service.py
templates/analysis.html
prompts/analyze_evidence.txt
```

담당 기능은 다음과 같음.

- LLM 호출
- 시간순 분석
- 모순·누락 탐지
- 제출자료 내용 생성

## D

```
routes/result.py
services/report_service.py
templates/index.html
templates/result.html
static/css/style.css
```

담당 기능은 다음과 같음.

- 전체 디자인
- 최종 결과
- 제출자료 내려받기
- 전체 화면 연결
- 배포

공통 파일인 `app.py`와 `requirements.txt`는 상의 없이 수정하지 않도록 정함.

# 실제 작동 구조

```
사용자가 HTML 화면 접속
→ Flask가 JSON 카드거래를 읽음
→ 거래 조건을 Python으로 판정함
→ 사용자가 합성 계약서를 선택함
→ Flask가 CLOVA OCR을 호출함
→ Flask가 추출 텍스트를 LLM API에 전달함
→ LLM이 시간순 관계·모순·누락을 반환함
→ Python 규칙 엔진이 기본요건을 확인함
→ Flask가 결과를 HTML에 전달함
→ Jinja2가 결과 페이지를 렌더링함
```

예시는 다음과 같음.

```
@app.route("/case/<case_id>/result")
def result(case_id):
    case_data = load_case(case_id)
    analysis = load_analysis(case_id)
    rule_result = evaluate_eligibility(case_data, analysis)

    return render_template(
        "result.html",
        case=case_data,
        analysis=analysis,
        rule_result=rule_result
    )
```

HTML에서는 다음처럼 결과를 출력함.

```
<h2>{{ rule_result.status }}</h2>

<ul>
  {% for reason in rule_result.reasons %}
    <li>{{ reason }}</li>
  {% endfor %}
</ul>
```

# 데이터베이스

MySQL은 사용하지 않아도 됨.

```
합성 카드거래
→ JSON 파일

대표 시나리오
→ JSON 파일

AI 프롬프트
→ TXT 파일

판정 규칙
→ Python 코드

사용자 입력
→ 실행 중 임시 처리
```

회원가입과 장기 사례 저장을 구현하지 않으므로 데이터베이스를 추가할 이유가 적음.

# JavaScript가 필요한 부분

HTML만으로 화면을 만들 수는 있지만 다음 기능에는 JavaScript가 필요함.

- 파일 선택 후 미리보기
- OCR 분석 중 로딩 표시
- 비동기 AI 호출
- 오류 메시지
- 결과 다시 분석
- sessionStorage
- 제출자료 내용 수정
- 인쇄 버튼

React까지 사용할 필요는 없고 Vanilla JavaScript로 충분함.

# 디자인

Bootstrap을 사용하면 다음 요소를 빠르게 만들 수 있음.

- 모바일 반응형
- 카드거래 목록
- 버튼
- 진행 단계
- 경고창
- 입력 폼
- 결과 카드
- 모달

CSS를 처음부터 전부 작성하는 것보다 빠르고, 네 명이 만든 화면의 모양을 통일하기도 쉬움.

# 배포

Flask는 Render에서 GitHub 저장소를 연결하고 `gunicorn app:app`으로 배포할 수 있음. [Render Flask 배포 가이드](https://render.com/docs/deploy-flask)

Vercel도 Flask를 공식 지원하며 Flask 앱을 하나의 서버 함수로 배포할 수 있음. 다만 Python 런타임이 현재 베타이고, 요청·응답 크기가 4.5MB로 제한되므로 문서 파일을 3MB 이하로 제한해야 함. [Vercel Flask 안내](https://vercel.com/docs/frameworks/backend/flask), [Vercel Functions 제한](https://vercel.com/docs/functions/limitations)

배포 플랫폼은 다음처럼 판단하면 됨.

- Flask를 가장 일반적인 방식으로 배포하려면 Render 사용
- Vercel을 이미 사용해봤고 파일 크기를 제한할 수 있으면 Vercel 사용
- Render 무료 서버는 일정 시간 사용하지 않으면 내려갈 수 있으므로 심사 안정성이 중요하면 유료 인스턴스나 Vercel을 검토함

# 최종 결론

팀 상황을 반영한 최종 추천 스택은 다음임.

```
Python Flask
+ Jinja2
+ HTML/CSS
+ Vanilla JavaScript
+ Bootstrap
+ JSON
+ LLM API
+ CLOVA OCR
+ Python 규칙 엔진
+ GitHub
+ Render 또는 Vercel
```